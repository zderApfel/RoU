extends RigidBody3D
@onready

var SELF = Item.new().init("two_hand_firearm", "Rifle", null, false, 1, true, 1)

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	pass
